- @questions.each do |question|
  .row
    .col-12
      = question.text
    .col-12
      // = form_for (@answer) do |f|
      //   = f.label :text
      //   = f.text_field :text
      //   = f.submit "送信する"

= paginate @questions
